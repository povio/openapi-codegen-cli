export const keys = {
    {{#each endpoints as | endpoint |}}
    {{endpointName endpoint}}: ({{#if (endpointParams endpoint)}}data: { {{{genEndpointParams endpoint}}} }{{/if}}) => ["{{endpoint.path}}", {{#if (endpointParams endpoint)}}data{{/if}}] as const,
    {{/each}}
};